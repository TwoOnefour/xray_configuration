{
    "inbounds": [
        {
            "port": 443,
            "protocol": "vless",
            "settings": {
                "clients": [
                    {
                        "id": "", // 填写你的uuid, 可以使用 `xray uuid` 生成
                        "flow": "xtls-rprx-vision"
                    }
                ],
                "decryption": "none"
            },
            "streamSettings": {
                "network": "tcp",
                "security": "reality",
                "realitySettings": {
                    "target": "www.microsoft.com:443", // 默认，不想过度配置则保持默认，不建议使用，若你想修改，建议使用邻居的
                    "serverNames": [
                        "www.microsoft.com" // 默认，不想过度配置则保持默认，不建议使用，若你想修改，建议使用邻居的
                    ],
                    "privateKey": "${private_key}", // 可以使用xray x25519生成，填入privateKey
                    "mldsa65Seed": "", // 可以使用xray mldsa65生成，填入生成的seed, 可以留空，则服务端不开启mldsa65
                    "shortIds": [
                        "" // 可留空，或者填写，可使用`openssl rand -hex 8`生成
                    ]
                }
            }
        }
    ],
    "outbounds": [
        {
            "protocol": "freedom",
            "tag": "direct"
        }
    ]
}
